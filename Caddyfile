# MamVibe production reverse proxy
# Caddy auto-obtains and renews Let's Encrypt TLS certificates.
# Set DOMAIN=mamvibe.com in your .env file.

{$DOMAIN} {
    reverse_proxy frontend:8080
}

# Redirect www → apex
www.{$DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}
